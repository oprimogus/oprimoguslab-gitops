apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-s3
  namespace: servarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 80Gi
  storageClassName: nfs-s3