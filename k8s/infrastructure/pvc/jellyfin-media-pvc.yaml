apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-media-storage-2
  namespace: servarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3600Gi
  storageClassName: nfs-media
  volumeName: nas-media-storage-pv